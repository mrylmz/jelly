(
    ROOT = AST_FUNC(
        SIGNATURE = AST_FUNC_SIGNATURE(
            NAME = AST_IDENTIFIER(
                TEXT = STRING(main)
            )
            PARAMETERS = [

            ]
            RETURN_TYPE = AST_TYPE(
                KIND = AST_IDENTIFIER(
                    TEXT = STRING(Void)
                )
            )
        )
        BLOCK = AST_BLOCK(
            STATEMENTS = [
                AST_BINARY(
                    OPERATOR = .
                    LEFT = AST_BINARY(
                        OPERATOR = .
                        LEFT = AST_IDENTIFIER(
                            TEXT = STRING(myStruct)
                        )
                        RIGHT = AST_IDENTIFIER(
                            TEXT = STRING(myMember)
                        )
                    )
                    RIGHT = AST_IDENTIFIER(
                        TEXT = STRING(myOtherMember)
                    )
                )
            ]
        )
    )
)
